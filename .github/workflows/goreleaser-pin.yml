name: Goreleaser placeholder

# Pin for future goreleaser-driven releases. This workflow does nothing now,
# but exists so CI can reference a release flow later.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Goreleaser workflow placeholder. Configure goreleaser.yml and enable this workflow when ready."
